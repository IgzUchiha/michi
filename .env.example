# Server Configuration
PORT=8000
API_BASE_URL=http://127.0.0.1:8000

# Database Configuration
# For local development (PostgreSQL):
DATABASE_URL=postgresql://localhost/rustaceaans
# For production (get from Railway/Heroku):
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# Storage Configuration
# Options: "local" or "s3"
STORAGE_TYPE=local
UPLOAD_DIR=./uploads

# AWS S3 Configuration (if using S3)
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key
# AWS_REGION=us-east-1
# S3_BUCKET=rustaceaans-uploads

# Cloudinary Alternative (if using Cloudinary instead of S3)
# CLOUDINARY_URL=cloudinary://key:secret@cloud_name

# Security
# JWT_SECRET=your_random_secret_here_change_in_production

# CORS
# ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
